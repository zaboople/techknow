package hello_utils

import "fmt"

/* This is meant to be used by the hello program, which is in the main package but this in the hello_utils package */
func Twerpy() {
	fmt.Printf("Hello from twerpy")
}